# 审查结果

本次只修订迁移计划文档，未修改业务代码。

## Critical

无。

## Warning

无。

## Info

- 已将计划中的文件夹命名从 `lz` 调整为 `supervision`。
- 已将布局组件建议名从 `LzAppLayout.vue` 调整为 `SupervisionAppLayout.vue`。
- 已将子目录 `lz-info` / `lz-work` 调整为 `integrity-info` / `integrity-work`。
