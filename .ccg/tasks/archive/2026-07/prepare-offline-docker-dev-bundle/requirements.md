# Requirements

- Prepare for an offline intranet development machine with similar configuration to the current PC.
- The intranet machine cannot access the internet.
- Docker startup should work the same way as the current local environment.
- Identify required Docker images, installers, local database/config prerequisites, and startup steps.
- Generate an operation document for the offline setup.
- Download or export required artifacts where practical.
