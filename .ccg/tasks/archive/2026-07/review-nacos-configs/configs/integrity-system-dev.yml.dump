--- META dataId=integrity-system-dev.yml group=DEFAULT_GROUP tenant= type=yaml
spring:
  data:
    redis:
      host: redis	# docker compose: internal redis
      port: 6379
      password:
  datasource:
    druid:
      stat-view-servlet:
        enabled: true
        loginUsername: admin
        loginPassword: 123456
    dynamic:
      druid:
        initial-size: 5
        min-idle: 5
        maxActive: 20
        maxWait: 60000
        connectTimeout: 30000
        socketTimeout: 60000
        timeBetweenEvictionRunsMillis: 60000
        minEvictableIdleTimeMillis: 300000
        validationQuery: SELECT 1 FROM DUAL
        testWhileIdle: true
        testOnBorrow: false
        testOnReturn: false
        poolPreparedStatements: true
        maxPoolPreparedStatementPerConnectionSize: 20
        filters: stat,slf4j
        connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
      datasource:
          master:
            driver-class-name: dm.jdbc.driver.DmDriver
            url: jdbc:dm://${DM_HOST:localhost}:5236/INTEGRITY-CLOUD?schema=INTEGRITY-CLOUD
            username: SYSDBA
            password: IntegritySupervision0

mybatis-plus:
  type-aliases-package: com.integrity.system
  mapper-locations: classpath:mapper/**/*.xml

springdoc:
  gatewayUrl: http://localhost:8080/${spring.application.name}
  api-docs:
    enabled: true
  info:
    title: System API
    description: System API
    contact:
      name: Integrity-Supervision-Platform
      url: https://integrity-supervision-platform.local
--- META dataId=integrity-system-dev.yml group=DEFAULT_GROUP tenant=public type=yaml
spring:
  data:
    redis:
      host: redis	# docker compose: internal redis
      port: 6379
      password:
  datasource:
    druid:
      stat-view-servlet:
        enabled: true
        loginUsername: admin
        loginPassword: 123456
    dynamic:
      druid:
        initial-size: 5
        min-idle: 5
        maxActive: 20
        maxWait: 60000
        connectTimeout: 30000
        socketTimeout: 60000
        timeBetweenEvictionRunsMillis: 60000
        minEvictableIdleTimeMillis: 300000
        validationQuery: SELECT 1 FROM DUAL
        testWhileIdle: true
        testOnBorrow: false
        testOnReturn: false
        poolPreparedStatements: true
        maxPoolPreparedStatementPerConnectionSize: 20
        filters: stat,slf4j
        connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
      datasource:
          master:
            driver-class-name: dm.jdbc.driver.DmDriver
            url: jdbc:dm://${DM_HOST:localhost}:5236/INTEGRITY-CLOUD?schema=INTEGRITY-CLOUD
            username: SYSDBA
            password: IntegritySupervision0

mybatis-plus:
  type-aliases-package: com.integrity.system
  mapper-locations: classpath:mapper/**/*.xml

springdoc:
  gatewayUrl: http://localhost:8080/${spring.application.name}
  api-docs:
    enabled: true
  info:
    title: System API
    description: System API
    contact:
      name: Integrity-Supervision-Platform
      url: https://integrity-supervision-platform.local
