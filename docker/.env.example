# Copy this file to docker/.env before running docker compose.

COMPOSE_PROJECT_NAME=integrity
IMAGE_PREFIX=integrity
IMAGE_TAG=dev

# Offline base images. Import these with docker load before building in intranet.
JAVA17_IMAGE=eclipse-temurin:17-jre
NACOS_IMAGE=nacos/nacos-server:latest
REDIS_IMAGE=redis:latest
NGINX_IMAGE=nginx:latest
# Optional. Set this to your Dameng image and start compose with --profile dm.
DM_IMAGE=dm8_single:latest

# Nacos
NACOS_SERVER_ADDR=nacos:8848
NACOS_USERNAME=nacos
NACOS_PASSWORD=nacos

# Dameng. If DB runs outside compose, set DM_HOST to the host/IP reachable from containers.
# On Linux Docker Engine, host.docker.internal is mapped by compose through host-gateway.
DM_HOST=host.docker.internal
DM_PORT=5236
DM_USERNAME=SYSDBA
DM_PASSWORD=IntegritySupervision0
DM_CONFIG_DATABASE=INTEGRITY-CONFIG
DM_CONFIG_SCHEMA=SYSDBA
DM_DATABASE=INTEGRITY-CLOUD
DM_SCHEMA=INTEGRITY-CLOUD
DM_FLOW_DATABASE=INTEGRITY-CLOUD-FLOW
DM_FLOW_SCHEMA=INTEGRITY-CLOUD-FLOW

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
# Must match requirepass in docker/redis/conf/redis.conf when Redis password is enabled.
REDIS_PASSWORD=

# JVM
JAVA_OPTS=-Xms256m -Xmx512m -Dfile.encoding=UTF-8 -Duser.timezone=Asia/Shanghai
